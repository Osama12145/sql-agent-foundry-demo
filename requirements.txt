fastapi==0.139.0
uvicorn==0.51.0
azure-ai-projects
azure-identity
# azure-ai-projects imports httpx at module load but does not always pull it in,
# so pin it explicitly or the first import fails with ModuleNotFoundError.
httpx
pydantic==2.13.4
python-dotenv==1.2.2
streamlit==1.59.1
pandas==3.0.3
requests==2.34.2
pytest==9.1.1
